[
    {
        "name": "get_customer",
        "description": "Retrieve a customer's profile by customer ID (for example CUST-1) or by email address. Returns the customer's name, email, loyalty tier, and lifetime order count. Use this to look up who a customer is before answering account questions.",
        "inputSchema": {
            "type": "object",
            "properties": {
                "customer_id": {
                    "type": "string",
                    "description": "The customer ID (for example CUST-1) or the customer's email address."
                }
            },
            "required": [
                "customer_id"
            ]
        }
    }
]