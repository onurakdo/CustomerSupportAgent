# Save captured evidence here

Drop exported traces (`agentcore traces get <id> --output docs/evidence/<name>.json`), log excerpts, and console
screenshots into this folder. See [../evidence.md](../evidence.md) for the per-scenario capture commands.
